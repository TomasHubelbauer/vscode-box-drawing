# Change to using sets of text edits

Prove that compounding multiple edits within a single builder will result in one undo.

If yes, use that instead of pure-text algo as it should be more straightforward.
